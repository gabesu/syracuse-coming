console.log("Check hero.js for snippets");

/*
    * TimelineMax usage
    ! import { TimelineMax } from "gsap";
    let tl = new TimelineMax();
*/

/*
* Scrollmagic usage
! import { TimelineMax, Animation } from "gsap";
import ScrollMagic from "scrollmagic";

let controller = new ScrollMagic.Controller();
let scene = new ScrollMagic.Scene({
  triggerElement: ".element",
  triggerHook: 0.6,
  duration: "50%"
}).addTo(accountController); // ? add setTween(timeline) if you want to animate based on scroll
*/

/*
* Typesplit usage
! import "typesplit";
let headerText = new SplitType("#header", {
    split: "lines"
});
! Then call headerText.lines to use
*/
